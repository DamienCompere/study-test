<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "Study-Advisor / Test-study", root_path, class: "navbar-brand" %>
    </div>
    <% if current_user %>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "Home", root_path %></li>
        <% if current_user.username == "admin" %>
            <li> <a href="/users/new" title="Create a new user">Create a new user</a></li>
            <li> <%= link_to "Check all users", users_path %></li>
        <%end%>
            <li> <%= link_to "Check all bugs", bugs_path %></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Log Out", logout_path,  title: "lougout" %></li>
        <li><a href="#"><span class="glyphicon glyphicon-user"></span> Logged in as <%= current_user.username %>.</a></li>
    </ul>
    <%end%>
  </div>
</nav>